PyLearn Images Folder
=====================

This folder is reserved for image assets if the project is expanded later.

Current project note:
- The current Part B submission uses CSS-based shapes, cards, and color styling instead of external image files.
- This keeps the client-side project lightweight and easy to run by opening index.html directly in the browser.
